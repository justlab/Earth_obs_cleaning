---
title: "Running `Earth_obs_cleaning` on MCD19A2: Results from CONUS"

echo: FALSE
df-print: kable
---

```{r}
source.files = "globals libraries util"
for (filename in strsplit(source.files, " ")[[1]])
     source(sprintf("code/%s.R", filename))
```

Some text before the table.

```{r table_cv_results}
pretty.table.numbers(tar_read(cv.summary)[1, -"Year"])
```

Some text after the table.
